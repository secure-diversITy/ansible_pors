- name: Settle up the base
  hosts: "*:!pors_server"
  remote_user: "{{ pors_ssh_user }}"
  become: yes
  become_user: root
  strategy: free

  roles:
    - system_upgrade
    - system_kernel
    - system_base
    - system_optimize
    - system_ntp
    - system_journal
