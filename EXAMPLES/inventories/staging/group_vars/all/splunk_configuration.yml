---
####################################################################################################################
#
# ALL OUTCOMMENTED KEY:VALUES (IF ANY) YOU SEE HERE ARE THE DEFINED DEFAULT VALUES
# i.e. there is no need to enable them if you like the shown default value
#
####################################################################################################################

#####################################################################################################################
# General Conf Variables
#####################################################################################################################

# directory of splunk's system/local
#splunk_conf_path: /opt/splunk/etc/system/local

# user/group splunk should run as
#splunk_install_user: splunk
#splunk_install_group: splunk

# optionally you can specify a uid and/or gid for the splunk install user
# default is the OS defines it automatically
#splunk_install_user_uid:
#splunk_install_user_gid: 

# overwrite the default shell for the the splunk install user:
# default is set in roles/system_user/defaults/main.yml
#splunk_install_user_shell: /bin/bash

# Should PORS push local splunk license(s) to the LicenseMaster?
# Licenses must be added to:
# {{ repo_base }}/{{ target_env }}/licenses/enterprise/
# e.g: /opt/pors_repo/staging/licenses/enterprise/
# (if using /opt/pors_repo and you are in the staging environment)
# Possible values: True | False
#lm_push_license: True
